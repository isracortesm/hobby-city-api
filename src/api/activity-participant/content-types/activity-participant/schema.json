{
  "kind": "collectionType",
  "collectionName": "activity_participants",
  "info": {
    "singularName": "activity-participant",
    "pluralName": "activity-participants",
    "displayName": "ActivityParticipant"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "accessCode": {
      "type": "uid",
      "required": true
    },
    "statusName": {
      "type": "enumeration",
      "required": true,
      "default": "registered",
      "enum": [
        "registered",
        "paid"
      ]
    },
    "checkIn": {
      "type": "boolean",
      "required": true,
      "default": false
    },
    "activity": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::activity.activity",
      "inversedBy": "participants"
    },
    "user": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "plugin::users-permissions.user"
    }
  }
}
